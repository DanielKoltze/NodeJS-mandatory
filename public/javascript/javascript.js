import main from "../assets/js/main.js"

main.typeAnimation(document.getElementById("code-snippet-example"), [
  "const multiply = (a , b) => a + b",
  "const { name, age } = person",
  "const foundNumbers = arr.filter(number => number > 10)",
  'import main from "../assets/js/main.js"',
])

